# ========================================
# 基本的なエイリアス
# ========================================

# エディタ
alias vim='nvim'
alias vi='nvim'

# その他
alias keen="cd ~/workspace/KeenDemo && docker compose up -d && cd ~/workspace/CHWorkforce"
