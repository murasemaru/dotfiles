# dotfiles - Windows パッケージ管理
# 使い方: 管理者権限のPowerShellで実行
#   Get-Content packages\win-choco.txt | Where-Object { $_ -notmatch '^#' -and $_ -match '\S' } | ForEach-Object { choco install $_ -y }

# 必須
git

# 開発ツール
vscode
wsl2

# ターミナル
# windows-terminal
# powershell-core

# オプション
# docker-desktop
# fzf
